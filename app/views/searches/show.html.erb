<h1>Search Result</h1>

<% if @search.search_books.empty? %>
	<p>No Redords Found</p>
<% else %>
	<% @search.search_books.each do |c| %>
		<div>
			<h1><%= c.name %></h1>
			<p>Price: $<%= c.price %></p>
			<p>Category: <%= c.category %></p>
			<p>ISBN: <%= c.isbn %></p>
		</div> 
	<% end %>
<% end %>

<%= link_to "Back", new_search_path %>